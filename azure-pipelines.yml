# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'


variables:
  imagename: 'test'
  registryname: 'acemnlpaquacr01.azurecr.io'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'Aqua Prod ACR'
    repository: '$(imagename)'
    command: 'build'
    Dockerfile: '**/Dockerfile'

- task: Docker@2
  inputs:
    containerRegistry: 'Aqua Prod ACR'
    command: 'login'

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: 'echo $AQUA_SCANNER_NAME'
    
- task: aquasecScanner@4
  inputs:
    image: '$(registryname)/$(imagename):$(Build.BuildId)'
    scanType: 'local'
    register: false
    hideBase: false
    showNegligible: false
    windowsScannerVersion: '4.2.0'
    scanner: 'acemnlpaquacr01.azurecr.io/aquasec/scanner:4.6.20342'
    runOptions: '--privileged -e SCALOCK_LOG_LEVEL=DEBUG'
    registry: 'testacr5'
    connection: 'Aqua Prod Scanner Info'
    customFlags: '--direct-cc'
    caCertificates: true
    scanMalware: true
    registerCompliant: true

- task: Docker@2
  inputs:
    containerRegistry: 'Aqua Prod ACR'
    repository: '$(imagename)'
    command: 'push'

